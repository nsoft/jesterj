Fundamental Objects:

Item - thing that a scanner found that might end up in the search index

Step - Something that needs to be done to an item before it can be sent to the search index

Scanner - Step that finds stuff that we might want to index.

Plan - a list of steps which when complete should either drop the item, or send it to the search index

Node - a JVM instance

System - The control web application